(define (length lst)
  (if (pair? lst)
    (+ 1 (length (cdr lst)))
    0))

(display (length '()))
(display (length '(1)))
(display (length '(1 2)))
(display (length '(1 2 3)))
(display (length '(1 2 3 4)))
